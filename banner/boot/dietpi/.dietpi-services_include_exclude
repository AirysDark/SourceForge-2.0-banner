# SourceForge 2.0-Services Include/Exclude configuration

# Include: Use "+ servicename" without quotes (") and hash (#) to let SourceForge 2.0
#   stop a custom service before software installs, updates, backups and other maintenance tasks
#   and restart ist afterwards automatically.
#+ myservice1
#+ myservice2

# Exclude: Use "- servicename" without quotes (") and hash (#) to prevent SourceForge 2.0 from
#   stopping a known service before software installs, updates, backups and other maintenance tasks.
#- cron
#- transmission-daemon
