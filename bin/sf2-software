#!/usr/bin/env bash
set -euo pipefail
FULL="/usr/local/share/sf2/software-full.sh"
if [[ -x "$FULL" ]]; then
  exec bash "$FULL"
else
  echo "[SF2] Missing: $FULL"
  echo "Put your full menu script there and run again."
  exit 1
fi
