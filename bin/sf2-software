#!/usr/bin/env bash
set -euo pipefail
trap 'sf2-banner >/dev/null 2>&1 || true' EXIT

FULL="/usr/local/share/sf2/software-full.sh"
if [[ -x "$FULL" ]]; then
  exec bash "$FULL"
else
  echo "Missing: $FULL"
  exit 1
fi
